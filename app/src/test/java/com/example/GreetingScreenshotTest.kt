package com.example

import org.junit.Assert.assertTrue
import org.junit.Test

/**
 * Placeholder unit test. Screenshot tests require Robolectric device setup
 * and are not required for the GroqApp deliverable.
 */
class GreetingScreenshotTest {
    @Test
    fun placeholder() {
        assertTrue(true)
    }
}
